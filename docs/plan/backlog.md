# Backlog

## Future Tasks

- Add richer output formats such as JSON and table rendering.
- Add more Gmail query filters such as unread, has attachment, and sender matching.
- Add message inspection actions beyond header listing.

## Planning Workflow

- Keep not-yet-started work here.
- Create new implementation plans as timestamped files in `docs/plan/`, for example `2026-05-02-add-json-output.md`.
- Move completed plans to `docs/plan/done/` using the same timestamped filename.
