## Goal

- Prepare `gmail-tool` for the `v0.3.1` release.

## Scope

- Bump the package version to `0.3.1`.
- Roll `CHANGELOG.md` `Unreleased` entries into a new `v0.3.1` section.
- Add `docs/release/v0.3.1.md` release notes.

## Plan

1. Update version metadata in `pyproject.toml`.
2. Move current unreleased entries into a dated `v0.3.1` changelog section.
3. Add focused release notes covering shipped changes, upgrade notes, and verification.
4. Run lightweight verification on changed release files.
